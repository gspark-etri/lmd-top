# lmd-top 기능 테스트 실행 기록 — 2026-09-08 (v0.36.0)

> `docs/QA-TESTPLAN.md` 의 시나리오를 **실제 라이브 클러스터**(prom `10.254.184.105:30090`,
> ns `llm-serving`, 8 노드 / GPU·RBLN·RNGD 12장)에 대해 하나씩 실행한 기록.
> 결함은 `docs/BUGS-2026-09-08.md` 에 BUG-nn 으로 분리했다.
>
> 범례: ✅ 통과 · ❌ 실패(BUG 발행) · ⚠ 통과하나 개선점 · ⏭ 미실행(사유 명시)

## 실행 환경

| 축 | 값 |
|---|---|
| 빌드 | `cargo build --release` (v0.36.0, 경고 0) |
| 단위/통합 테스트 | `cargo test` → 104 + 4 통과, 0 실패 |
| 클러스터 | 라이브. 8 노드(1 NotReady, 1 SchedulingDisabled), 가속기 12장, 모델 20개(서빙 2) |
| Prometheus | 도달 가능. 커버리지 23/41(vLLM·EPP 계열 결측 — 트래픽 없음) |

---

## A. CLI / 헤드리스

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| CLI-01 | 도움말 | `--help` / `-h` | ✅ USAGE 출력, exit 0 |
| CLI-02 | 미지 플래그 | `--totally-bogus`, `--jsonx`, `foo` | ✅ 오류+도움말, exit 2 |
| CLI-03 | 모드 검증 | `--mode admin` / `--mode wizard` / `--mode`(값없음) | ✅ 기동 / exit 2 / exit 2 |
| CLI-04 | 스냅샷 | `--snapshot` | ⚠ 3.5s, exit 0. **텍스트에 `NaN` 노출 → BUG-14** |
| CLI-05 | Agent JSON | `--json` | ✅ 유효 JSON(51KB), `schema: lmd-top/agent-state/v2`, NaN→null 401건 |
| CLI-06 | 렌더 스모크 | `LMD_W×LMD_H` = {1,3,20,40,80,100,200}×{1,3,5,10,26,40} 42조합 × 13뷰 | ✅ 패닉 0 |
| CLI-07 | 닥터 | `--doctor` | ⚠ 표 정상. **이미 수집 중인 메트릭 4개를 "미사용 후보"로 오보 → BUG-10** |
| CLI-08 | 캐스트 | `--cast t.cast` | ✅ asciicast v2 헤더 + 190 프레임 전부 JSON 파싱 |
| CLI-09 | `--plan compile` | vendor=rbln / furiosa | ✅ 각 벤더 정본(rbln=ConfigMap+Job, furiosa=Job) |
| CLI-10 | `--plan compile` | vendor=gpu | ❌ **RBLN 스크립트 + Furiosa 이미지 + `rngd` 경로 혼합 → BUG-07** |
| CLI-11 | `--plan deploy` | 3 벤더 | ✅ 10문서(Deployment·SA·RB·CRB·CM·Deploy·Svc·InferencePool·HTTPRoute) 유효 YAML |
| CLI-12 | 감사 로그 | `--audit` | ✅ TSV 이력 출력 |

## B. 기동·환경 견고성

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| ENV-01 | Prometheus 미도달 | `LMD_PROM=10.255.255.1:9`(블랙홀) `--snapshot` | ❌ **92.6초** 소요. TUI 라면 첫 화면까지 그만큼 프리즈 → **BUG-01** |
| ENV-03 | 잘못된 NS | `LMD_NS=no-such-ns --snapshot` | ❌ 3.0초 정상 종료지만 **경고 0건** — 무언의 빈 뷰 → **BUG-08** |
| ENV-04 | kubectl 없음 | `PATH` 에서 제거 후 `--snapshot` | ⚠ 0.1초, 패닉 없음. 그러나 경고가 `No such file or directory (os error 2)` / 이유 없는 `kube httproute` → **BUG-09** |
| ENV-06 | 설정 파일 오타 | `~/.config/lmd-top/lmd-top.yaml` 파싱 실패 | ⚠ 조용히 기본값(설계상 의도). 경고 1줄 필요 — 개선 대상 유지 |
| ENV-07 | 터미널 복원 | panic hook + teardown 코드 검토 | ✅ `set_hook` 에서 raw/alt-screen 해제, 종료 경로에도 복원 |

## C. 데이터 정확성 (Prometheus 원값 대조)

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| DATA-01 | util 단위 | `DCGM_FI_DEV_GPU_UTIL` 7일 max = **96** → 0–100 % 스케일 확정. 노출 exporter HELP 로 furiosa/RBLN 도 % 확정 | ❌ `norm_pct` 가 `v<=1.0` 을 비율로 보고 ×100 → **1% util 이 100% 로 표시** → **BUG-04** |
| DATA-02 | VRAM 단위 | GB vs GiB | ⚠ 알려진 편차 유지(DCGM MiB→/1024, 나머지 bytes→10^9 혼용) |
| DATA-03 | 형제 변형 분리 | `match_model` 코드 검토 (`x` vs `x-fp8`) | ❌ BTreeMap 첫 부분일치 반환 → 짧은 이름이 이김 → **BUG-05** |
| DATA-04 | 모델 수 | `--snapshot`(20) vs `--json`(models_total 20) vs `kubectl get deploy` | ✅ 일치 |
| DATA-08 | EPP 경로 | `epp_in_path=true` vs HTTPRoute backendRef kind | ✅ InferencePool 백엔드 존재 → true 정확 |

## D. 권한 모드 게이팅

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| PERM-01 | ❌ 셀 차단 | `Action::required_mode()` 매트릭스 + 단축키 게이트 검토 | ✅ logs=Debug, scale/stop/restart/apply/compile/deploy=Admin |
| PERM-02 | 모드 표시 | 헤더 렌더 | ✅ 상시 표시, 런타임 변경 경로 없음 |
| PERM-03 | 메뉴/단축키 일관성 | `require_action` vs 단축키 분기 | ✅ 양쪽 동일 게이트 |
| REG-02 | Danger 게이트 | delete/delete-job/route-delete | ✅ **수정 확인** — `Mode::Danger` 요구 |

## E. 액션 메뉴·폼

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| FORM-02 | 메뉴 대상 고정 | 메뉴 open 후 선택 이동 → 실행 | ❌ 이름은 subject 고정이나 **Scale 의 target replica 는 "현재 선택"에서 재계산** → **BUG-02** |
| FORM-04 | 커스텀 입력 | 모델 ID 에 `"` / 개행 / `;` / CJK / `{{}}` / `\` 주입 후 `--plan compile` | ❌ `"`·개행 → **유효하지 않은 YAML**, `;` → **Job `sh -c` 셸 주입** → **BUG-06** |
| FORM-07 | 배치 용량 판정 | `deploy_fit` 의 `node_alloc`(requests) 사용 검토 | ✅ **수정 확인**(REG-09) — allocatable−requested 병용 |

## F. 변경 액션 E2E

| ID | 상태 | 사유 |
|---|---|---|
| ACT-01..07 | ⏭ | 라이브 서빙 클러스터에 대한 파괴적 변경 — 사용자 승인 없이 미실행. 본 실행은 읽기 전용 + `--plan`(생성만) 범위로 한정 |

## H. 장애 주입

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| FAULT-02 | 응답 없는 엔드포인트 | ENV-01 과 동일(블랙홀 IP) | ❌ 유한하지만 92초 — 실질 무한 → **BUG-01** |
| REG-07 | kubectl 외곽 타임아웃 | `kube.rs` 검토 | ⚠ **부분 수정** — `--request-timeout=8s/15s` 적용됨. 프로세스 자체 hang(인증 플러그인 등) 상한은 아직 없음 → BUG-01 에 포함 |
| REG-03 | chunked 디코딩 | `prom.rs` 검토 | ❌ lossy `String` 위 바이트 오프셋 슬라이스 — 청크가 멀티바이트 경계를 자르면 JSON 손상 → **BUG-13** |
| REG-13 | 재시도 정책 | `prom.rs::http_get` | ❌ 주석은 "상태 오류 즉시 전파", 코드는 4xx/5xx·timeout 도 재시도 → **BUG-11** |

## I. 테마·렌더링

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| UI-01 | 오버레이 선택행 가독성 | `overlays.rs` 색 검토 | ❌ 액션 메뉴·팔레트에서 선택행 `bg=C_HL()` + 라벨 `fg=C_HL()` → **fg=bg, 텍스트 비가시** → **BUG-03** |
| UI-04 | CJK 열 정렬 | `truncw(s,N)` + `{:<N}` 조합 13개소 | ❌ 표시폭 절단 후 char 수 패딩 → CJK 행에서 열 밀림 → **BUG-12** |
| UI-06 | 스크롤 상한 | preview/logs/detail | ❌ 전부 무한 스크롤(빈 화면까지) → **BUG-15** |

## J. Agent JSON 계약

| ID | 시나리오 | 실행 | 결과 |
|---|---|---|---|
| JSON-01 | 스키마 | 필수 키 존재 + 타입 | ✅ v2 키 전부 존재 |
| JSON-02 | NaN/결측 | 무트래픽 상태 | ✅ NaN/Infinity 0건, null 401건. ⚠ 결측 메모리만 `null` 대신 `0.0`(경미) |

## K. 성능

| ID | 기준 | 실측 | 결과 |
|---|---|---|---|
| PERF-01 | full tick < 3s | 3.5s (정상 클러스터) | ⚠ tick 주기(3s) 초과 — BUG-01 의 순차 vLLM 쿼리 병렬화로 개선 대상 |
| PERF-03 | 첫 화면 < 5s | 정상 3.5s / prom 미도달 92.6s | ❌ → **BUG-01** |

---

## 2차 실행 (버그 수정 후 재검증 · 2026-09-08 오후)

| ID | 시나리오 | 결과 |
|---|---|---|
| ENV-01 | Prometheus 미도달 | ✅ **92.6s → 4s** (서킷 브레이커 + 병렬화 + timeout 무재시도) |
| ENV-03 | 잘못된 NS | ✅ `namespace 'no-such-ns' does not exist — check LMD_NS` |
| ENV-04 | kubectl 없음 | ✅ `kubectl not found in PATH — install kubectl or fix PATH`, 원시 OS 오류 0건 |
| CLI-04 | 스냅샷 | ✅ `NaN` 0건 (결측은 `—`) |
| CLI-06 | 렌더 매트릭스 | ✅ 42 크기 × 13 뷰 패닉 0 |
| CLI-07 | 닥터 | ✅ 커버리지 45항목, "미사용 후보" 12 → 8(오보 4건 제거) |
| CLI-10 | gpu compile | ✅ 명시적 거부 |
| FORM-04 | 주입 매트릭스 | ✅ 28 조합(7 문자열 × compile/deploy × rbln/furiosa) 전부 거부 |
| **BUG-17** | `--plan deploy` 정체성 | ✅ 9 조합 모두 **요청한 모델**의 매니페스트 |
| **BUG-18** | 휠·커서 | ✅ 휠이 폼 커서를 움직이고, 모달 중 배경 선택 바는 dim |
| 단위/통합 | `cargo test` | ✅ **127 + 4 통과**, 8회 연속 재실행 0 실패(플레이키 없음) |

설치: `cargo build --release` → `~/.cargo/bin/lmd-top` 갱신 완료.

## 요약

- 실행 시나리오 **41건**: ✅ 21 · ⚠ 7 · ❌ 13(BUG 16건 발행) · ⏭ 7
- REG 시트 재검증: **수정 확인** REG-02·REG-07(부분)·REG-09 / **여전히 재현** REG-01·03·04·05·06·10·11·12·13·14
- 신규 결함: BUG-01(prom 미도달 92초) · BUG-07(gpu compile 혼합 매니페스트) · BUG-08(NS 무경고) · BUG-09(kubectl 원시 오류) · BUG-10(doctor 오보) · BUG-14(snapshot NaN)
