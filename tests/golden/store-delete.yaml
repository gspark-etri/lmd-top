# title: delete compiled/Qwen--Qwen2.5-0.5B-Instruct/rbln/RBLN-CA22-tp4-s8192
# Store maintenance — delete compiled/Qwen--Qwen2.5-0.5B-Instruct/rbln/RBLN-CA22-tp4-s8192 (12G)
# Irreversible: the artifact is removed from the shared store.
# The inventory ConfigMap refreshes on the discovery CronJob's next run.
# rm -rf /mnt/store/compiled/Qwen--Qwen2.5-0.5B-Instruct/rbln/RBLN-CA22-tp4-s8192
apiVersion: batch/v1
kind: Job
metadata:
  name: store-rm-ed-qwen-qwen2-5-0-5b-instruct-rbln-rbln-ca22-tp4-s8192
  namespace: llm-serving
  labels:
    app.kubernetes.io/component: store-maintenance
    lmd-top/store-op: delete
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      restartPolicy: Never
      volumes:
      - name: store
        persistentVolumeClaim:
          claimName: model-store
      containers:
      - name: remove
        image: busybox:1.36
        command:
        - rm
        - -rf
        - --
        - /mnt/store/compiled/Qwen--Qwen2.5-0.5B-Instruct/rbln/RBLN-CA22-tp4-s8192
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            memory: 256Mi
        volumeMounts:
        - name: store
          mountPath: /mnt/store
